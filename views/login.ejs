<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>게시판</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">

</head>
<body>
    <div class="mx-auto p-2" style="width: 1400px;">
    <h4 class="container mt-5"><strong>로그인 페이지</strong></h4>
    <div class="container mt-4">
        <form action="/login" method="post">
            <div class="form-group my-4">
                <label class="mb-2">ID</label>
                <input type="text" class="form-control" name="id">
            </div>
            <div class="form-group my-4">
                <label class="mb-2">password</label>
                <input type="password" class="form-control" name="pw">
            </div>
            <button type="submit" class=" btn btn-primary fw-bold">확인</button>
            <button type="submit" class=" btn btn-danger regsiter">회원가입</button>
            </form> 
        </div>
</div>
    </body>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
        crossorigin="anonymous"></script>
<script>
    $(document).ready(function () {
        $('.regsiter').click(function (e) {
            let clicked = $(this);
        $.ajax({
            method: 'GET', // 사용할 HTTP 메서드를 지정합니다.
            url: '/regsiter', // 동적인 매개변수를 직접 지정합니다.
        }).done(function (result) {
            window.location.href = '/regsiter';
            console.log('실행완료', result);
            
        }).fail(function (err) {
            console.log('에러', err);
        });
    });
});

</script>
</html>
